{
  "name": "openai2claude-proxy",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "openai2claude-proxy": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^2.1.8"
  }
}
